%!TEX root = ../template.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% chapter5.tex
%% NOVA thesis document file
%%
%% Chapter with Validation and Testing
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\typeout{NT FILE chapter5.tex}

\chapter{Verification and Testing}
\label{cha:verificaton_testing}

In this chapter we'll go over the verification and testing procedures conducted to evaluate the proposed wearable monitoring system and the \gls{ML} models. This evaluation is divided into three sections addressing the full \gls{IoT} system.
Starting with the hardware, we examine the position of the prototypes, and how they are attached, and the anatomical placement of the \gls{EMG} electrodes on the Gastrocnemius Lateralis. Secondly, an in-depth statistical analysis of the trained 
\gls{ML} models is conducted. In this section, by leveraging classification metrics, confusion matrices, and feature importance distributions, both developed \gls{ML} models' performances are compared against each other and evaluated. Here, 
we also determine the efficacy of fusing \gls{IMU} kinematics with \gls{EMG} neuromuscular data for basketball movements classification. In the final section, the system's performance is reviewed by assessing both models' latency and the resilience 
to asynchronous \gls{BLE} transmissions.

\section{Data Collection}
\label{sec:data_collection}

The data sets used to train and test the models were collected from several volunteer athletes. They were asked to do each of the five movements - Idle, Walking, Running, Defensive Slide and Rebounding - for some minutes 
each. The cramping prevention system was only tested on one player that pushed his muscles to near involuntary contractions.


\subsection{Prototype Placement and Configuration}
\label{ssec:prototype_placement}

The prototypes were mounted directly in the athletes' footwear to approximate the ideal positioning (on the bottom of the feet as an insole or a sock). The devices were secured using the shoe laces and the powerbank was secured in the users 
socks, utilizing the existing infrastructure. With this configuration, the \gls{IMU} accurately captures the kinematics of the foot, such as accelerations and rotational velocities during the athletes' movements. Furthermore, this setup 
minimizes the mechanical strain exerted by the power cables on the USB ports, while keeping the \gls{EMG} leads in close proximity to the chosen muscle group.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.5\linewidth]{deviceInPlace.jpg}
  \caption{An athlete wearing the prototype}
  \label{fig:protoInPlace}  
\end{figure}


The muscle chosen to be measured was the Gastrocnemius Lateralis as it is one of the most important muscles for explosive jumping motion.
The position for the \gls{EMG} sensors was chosen as recommended by the \gls{EU}'s SENIAM (Surface ElectroMyoGraphy for the Non-Invasive Assessment of Muscles) project (~\cite{emgPosition}) ensuring standardization and maximization of the 
signal-to-noise ratio.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.6\linewidth]{emgPosition.jpg}
  \caption{Gastrocnemius Lateralis Positioning ~\cite{emgPosition}}
  \label{fig:emgPosition}
\end{figure}

%\subsection{Collecting Data and Testing}
%\label{ssec:prototype_placement}

\section{Machine Learning Model Evaluation}
\label{sec:ml_evaluation}

This section goes over an evaluation of the two \gls{ML} models trained for this study. To effectively assess their performance, stability, and learning behaviour, we will examine the graphics generated throughout 
the training phase giving us clear insights into how each model converges, identifying potential issues like under or overfitting, and ultimately compare their suitability for the objectives of this project.


\subsection{Training Metrics and Performance Curves}    
\label{ssec:training_metrics}

For the TinyML model, the learning process is monitored iteratively across the training epochs. Figure~\ref{fig:tinyMLAccuracy} illustrates the model's accuracy progression, comparing the training dataset against the 
unseen validation dataset. As observed in the accuracy curve, the model learns rapidly during the initial epochs before stabilizing reaching an validation accuracy of approximately 94\%. The validation curve 
remains close to the training line throughout the training epochs indicating that the model generalizes well to new data rather than simply memorizing the training samples.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\linewidth]{TinyMLAccuracy.jpg}
  \caption{TinyMLAccuracy}
  \label{fig:tinyMLAccuracy}
\end{figure}

Figure~\autoref{fig:tinyMLLoss} further demonstrates that behaviour. The categorical cross-entropy loss curve shows a synchronized descent of both the training and validation loss curves confirming that the model is
successfully minimizing its errors during the training and attributing high confidence to the correct movement.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\linewidth]{TinyMLLoss.jpg}
  \caption{TinyMLLoss}
  \label{fig:tinyMLLoss}
\end{figure}

While the Neural Network was evaluated via epoch progression, the Cloud-based Random Forest model is evaluated using ensemble classification metrics, specifically the Receiver Operating Characteristic and 
Precision-Recall curves.

The Receiver Operating Characteristic curve (~\autoref{fig:RFROC}) evaluates the true positive rate against the false positive rate for each individual movement class. The Area Under the Curve scores approach 
1.0 across all movements, demonstrating that the \gls{RF} algorithm is highly capable of distinguishing the mathematical boundaries between distinct biomechanical states.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.87\linewidth]{RFROC.jpg}
  \caption{RF ROC Curve}
  \label{fig:RFROC}
\end{figure}

The Precision-Recall curve (~\autoref{fig:RFPrecision}) shows the trade-off between precision and recall for different thresholds. This type of chart is specially crucial in in highly imbalanced sports datasets 
where the vast majority of movements recorded are Walking, Running or Defensive Sliding while movements like Rebounds, Shooting and Crossover happen in a split second. The resulting curve demonstrates that the \gls{RF} 
model is able to detect all movements with high precision and low false positives.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\linewidth]{RFPrecisionRecall.jpg}
  \caption{RF Precision Recall Curve}
  \label{fig:RFPrecision}
\end{figure}


\subsection{Feature Importance and Sensor Fusion}
\label{ssec:feature_importance}

The \gls{RF} approach utilizes decision trees so it inherently provides a quantifiable measure of how much each sensor contributes to the final classification, known as Gini Feature Importance.

The next ~\autoref{fig:featureImportance} shows that even the \gls{IMU} data has greater importance and the \gls{EMG} provides a very small contribution to the movement identification (+/-10\%). This means that 
for better performance the \gls{EMG} data could be cut and be used only for the injury prevention algorithm.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=1\linewidth]{featureImportance.jpg}
  \caption{Aggregated Feature Importance Graph}
  \label{fig:featureImportance}
\end{figure}

As illustrated in ~\autoref{fig:featureImportance}, the \gls{EMG} features \gls{VAR} and \gls{RMS} had the biggest impact. This shows that the \gls{EMG} is valuable for movement recognition systems alongside the 
\gls{IMU}. The graph also shows a preference for the left side suggesting that the model picked up on a dominant players' foot.

\subsection{\gls{ML} Training - Confusion Matrices and Error Analysis}
\label{ssec:confusion_matrices_ML}

To conduct an analysis of the models' predictive flaws, confusion matrices were generated for both the Edge and Cloud classifiers based on the offline test dataset.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\linewidth]{tinyMLConfusionMatrix.jpg}
  \caption{TinyML Confusion Matrix}
  \label{fig:tinyMLConfusionMatrix}
\end{figure}


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\linewidth]{RFConfusionMatrix.jpg}
  \caption{Random Forest Confusion Matrix}
  \label{fig:RFConfusionMatrix}
\end{figure}


\autoref{fig:tinyMLConfusionMatrix,fig:RFConfusionMatrix} reveal strong diagonal density confirming high accuracy overall. However, looking at the errors reveal that most of the misclassification occurs between 
movements that share similar profiles (for example the models sometimes confuse the movement Defensive Sliding with Running and vice-versa or Rebound with Idle). 


\section{System Performance} %rever
\label{sec:system_performance}

A live test was performed in similar fashion to the data collection phase with the simple difference of running the server and taking note of the predictions and the movement made by the athlete (using the "Send Predictions" switch). The 
player also worked intensively until the fatigue warning triggered.


\subsection{Overall System Accuracy}
\label{ssec:overall_accuracy}

The primary objective of this test was to compare the classification accuracy of the ultra-lightweight Edge model against the deeper Cloud model and the variation of smoothed predictions vs raw. In terms of latency, 
the tinyML model responded instantly while the \gls{RF} model had a slight delay.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\linewidth]{testOverallAccuracyComparison.jpg}
  \caption{Overall Classification Accuracy: Local TinyML vs Cloud Random Forest (Raw and Smoothed)}
  \label{fig:overall_accuracy}
\end{figure}

As illustrated in ~\autoref{fig:overall_accuracy}, the localized TinyML model successfully outperformed the cloud-based Random Forest model, achieving an overall accuracy of 79.5\% compared to the server's 
71.2\%. Even tho the local model achieved better results, with a greater dataset the server model should outperform it, especially with more confusing movements. The smoothed version of the local model has a 6.4\% 
higher accuracy than its counterpart while the server's is worse by 0.3\%. This shows that smoothing the predictions is a viable and worthwhile algorithm if the base model is consistent enough.

\subsection{Real Test - Confusion Matrices and Error Analysis}
\label{ssec:confusion_matrices_test}

In ~\autoref{fig:movement_accuracy}, we can see that overall, besides Defensive Sliding, the \gls{RF} model either outperformed the TinyML model or was nearly as accurate. The \gls{RF} model struggled to correctly 
identify the movements Rebounding and Defensive Sliding but excelled at the Idle, Walking and Running movements. The improvement from the smoothing algorithm is also prevalent here in the majority of the movements.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\linewidth]{testMovementAccuracy.jpg}
  \caption{Classification Accuracy by Movement}
  \label{fig:movement_accuracy}
\end{figure}

Continuing the system's assessment, the confusion matrices for both models were generated:
 
\begin{figure}[htbp]
  \centering
  \includegraphics[width=.7\linewidth]{testConfusionMatrixRFRaw.jpg}
  \caption{Confusion Matrix for the Random Forest Model before Smoothing}
  \label{fig:confusion_matrix_RF_Raw}
\end{figure}

The \gls{RF}'s matrices (~\autoref{fig:confusion_matrix_RF_Raw},~\autoref{fig:confusion_matrix_RF_Smooth}) are very similar showing that when the athlete was Defensive Sliding, the model predicted the movement Running or Walking 
while only predicting about 12\% of the time right. However, the model has a very high accuracy on the movements Walking, Idle and Running while Rebounding was mistaken with Walking or Running half the time.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=.7\linewidth]{testConfusionMatrixRFSmoothed.jpg}
  \caption{Confusion Matrix for the Random Forest Model after Smoothing}
  \label{fig:confusion_matrix_RF_Smooth}
\end{figure}


On the other hand, the TinyML model had a slight accuracy boost by the smoothing algorithm. Analysing ~\autoref{fig:confusion_matrix_TinyML_Raw} and in~\autoref{fig:confusion_matrix_TinyML_Smooth}, the edge model 
also mistaken Running with Defensive Sliding and vice-versa as the \gls{RF} model did. Even so, the matrix's diagonal is pretty strong and the movements Walking and Idle and phenomenal having a near perfect accuracy.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=.7\linewidth]{testConfusionMatrixTinyMLRaw.jpg}
  \caption{Confusion Matrix for the Raw TinyML Model}
  \label{fig:confusion_matrix_TinyML_Raw}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\linewidth]{testConfusionMatrixTinyMLSmoothed.jpg}
  \caption{Confusion Matrix for the Smoothed TinyML Model}
  \label{fig:confusion_matrix_TinyML_Smooth}
\end{figure}


\section{Discussion of Results}
\label{sec:discussion} 

Even tho the ultimate goal of using the system in a real game scenario wasn't conducted in this project, the results validate the core hypothesis of the study that combining \gls{IMU} with \gls{EMG} data enhances 
movement recognition while also highlighting the major obstacles for that objective. 

Another objective of this research was to determine if the inclusion of \gls{EMG} sensors provided a tangible advantage over traditional \gls{IMU}-only commercial wearables. The Feature 
Importance analysis (\autoref{ssec:feature_importance}) definitively answered this question, as the Random Forest classifier relied heavily on the \gls{EMG} \gls{VAR} and \gls{RMS} features to 
differentiate complex basketball movements.


The implementation of the 500ms Majority Voting sliding window raises the challenge of balancing output stability and sensitivity of the models. As observed in the confusion matrices (\autoref{ssec:temporal_smoothing_analysis}), 
temporal smoothing acted as an effective low-pass filter, diminishing flickering and hardware noise for the predictions increasing its accuracy at the cost of responsiveness. This trade-off is acceptable for a system 
focused on general fatigue and activity tracking, but in future iterations with different goals it should be avoided.


\subsection{Known Problems and System Limitations}
\label{ssec:problems}

The transition from a controlled laboratory environment to live, dynamic basketball testing introduced several unforeseen hardware and methodological challenges. Documenting these limitations is critical for 
contextualizing the dataset's quality and guiding future iterations of the prototype.

\subsubsection{Hardware Fragility and Ergonomics}
\label{ssec:problems_hardware}
The explosive nature of basketball movements places immense mechanical stress on external wearable components. During early testing, the \gls{USB} connections, specifically the left Arduino port and the right 
\gls{EMG} port, could not withstand the forces generated by the wires during the movements and were physically ripped from their boards. These accidents required re-soldering to restore functionality. 
Furthermore, the mechanical coupling of the devices to the athlete was imperfect. The prototypes were not entirely secured under maximum tension, occasionally allowing the modules to shift out of their initial 
alignment upon heavy foot strikes, which introduced mechanical noise into the \gls{IMU} readings. This led to some internal hardware damage to the left Arduino's Bluetooth module restricting the antennas' effective 
range. Since the system pushed heavy data payloads at a high frequency (50Hz), the degraded \gls{BLE} module dropped packets whenever the Android device wans't in close-range proximity to the foot. This heavily 
reduced the overall quality of the collected data for the left foot.
Ergonomically, the custom keep-awake circuit required to bypass the power banks' auto-shutoff feature caused slight scratching against the athletes' legs, indicating that future prototypes require softer, fully 
integrated enclosures.


\subsubsection{Dataset and Labeling Constraints}
\label{ssec:problems_data}

From a machine learning and data science perspective, the collected dataset faces generalizability constraints. First, the data collection was limited to a sample size of only two individuals (the primary researcher 
and one additional subject). Consequently, the trained models may be highly tuned to the specific biomechanical signatures of these two users, requiring a much larger demographic pool to achieve universal accuracy.
Secondly, the manual labeling process introduced boundary noise. Because recording was triggered manually via the Android application, the initial and final seconds of many data windows inadvertently captured 
transitional movements. This minor overlap injected small instances of mislabelled data into the training pipeline.
Finally, the collection of physiological fatigue data presented a significant challenge. Safely and reliably inducing and capturing a pre-cramp state in a testing environment is inherently difficult. 
As a result, gathering stable, sustained \gls{RMS} baseline values for the peak fatigue threshold was challenging, making the deterministic cramping algorithm highly sensitive and difficult to calibrate 
with the current limited dataset.



